<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Catálogo de Productos</title>

  <style>
    * { box-sizing: border-box; }

    body {
      font-family: Arial, sans-serif;
      margin: 0;
      color: #333;
    }

    .pagina {
      padding: 20px 24px;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: repeat(3, 1fr);
      gap: 14px 18px; /* un poco más de espacio */
      height: 100vh;
      page-break-after: always;
    }

    .producto {
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 10px; /* más espacio */
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      text-align: center;
      background-color: #fafafa;
    }

    .producto img {
      width: 100%;
      max-height: 160px; /* MÁS GRANDE */
      object-fit: contain;
      margin-bottom: 8px;
    }

    .producto h3 {
      font-size: 13px; /* más grande */
      color: #007bff;
      margin: 4px 0 6px 0; /* más espacio */
      line-height: 1.2;
      max-height: 36px;
      overflow: hidden;
    }

    .producto p {
      font-size: 11px; /* subimos de 9px a 11px */
      margin: 3px 0; /* más aire entre líneas */
      line-height: 1.25; /* mejor legibilidad */
    }

    .precio {
      font-weight: bold;
      color: #2c7a00;
      margin-top: 4px;
    }

    .stock {
      font-weight: 600;
      margin-top: 2px;
    }
  </style>

</head>
<body>
  <% for (let i = 0; i < productos.length; i += 9) { %>
    <div class="pagina">
      <% productos.slice(i, i + 9).forEach((p) => { %>
        <div class="producto">
          <img src="<%= p.imagenUrl %>" alt="Imagen del producto" />

          <h3><%= p.nombre %></h3>

          <p><strong>Categoría:</strong> <%= p.categoria?.nombre ?? "N/A" %></p>

          <p class="precio">
            <strong>Precio:</strong> $<%= p.precioVenta.toLocaleString("es-CO") %>
          </p>

          <p class="stock">
            <strong>Stock:</strong> <%= p.stockDisponible %>
          </p>
        </div>
      <% }) %>
    </div>
  <% } %>
</body>
</html>
